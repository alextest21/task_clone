<div class="page-header">
	<h1>Categories <%= link_to 'Add Category', new_admin_category_path, class: "btn btn-success page-header-button" %></h1>
</div>
<div class="table-responsive">
	<table class="table table-striped">
		<thead>
			<tr>
				<th>Id</th>
				<th>Name</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			<% @category.each do |n| %>
			<tr>
				<td><%= n.id %></td>
				<td><%= n.name %></td>
				<td class="col-md-1">
					<%= link_to edit_admin_category_path(n) do %>
					<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
					<% end %>
					<%= link_to admin_category_path(n), method: :delete,
					data: { confirm: 'Are you sure?' } do %>
					<span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
					<% end %>

				</td>
			</tr>
			<% end %>
		</tbody>
	</table>
</div>